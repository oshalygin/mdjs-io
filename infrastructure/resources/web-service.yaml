apiVersion: v1
kind: Service
metadata:
    labels:
      app: fe-web
    name: fe-web
spec:
    type: LoadBalancer
    ports:
      - port: 80
        targetPort: 8080
        protocol: TCP
    selector:
      app: fe-web
